export enum ROUTE {
  LANDING = "/",
  SIGNIN = "/login",
  SIGNUP = "/signup",
  ONBOARDING = "/onboarding",
  DASHBOARD = "/dashboard",
  TRANSACTIONS = "/transactions",
  PROFILE = "/profile",
  GROUP_DETAILS = "/groups/:groupId"
}
